name: zenith_account_server
description: Zenith Account Server for device discovery and auto-pairing
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.1.1
  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.10.0

dev_dependencies:
  lints: ^4.0.0
